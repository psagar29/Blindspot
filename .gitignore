# Dependencies and build output
node_modules/
dist/
coverage/
*.tsbuildinfo

# Local credentials — never commit real env values (blank .env.example is allowed)
.env
.env.*
!.env.example
*.local

# OS / editor noise
.DS_Store
Thumbs.db

# Local working files
.agent-work/
